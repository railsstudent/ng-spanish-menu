import { RenderOptionPipe } from './render-menu-option.pipe'

describe('RenderOptionPipe', () => {
  it('create an instance', () => {
    const pipe = new RenderOptionPipe()
    expect(pipe).toBeTruthy()
  })
})
